[tool.poetry]
name = "modern-tui"
version = "0.0.0"
description = "Textual-based TUI app split into a package"

[tool.poetry.dev-dependencies]
pytest = "^7.0"

[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-q"
testpaths = ["tests"]
